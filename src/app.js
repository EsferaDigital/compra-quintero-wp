// Importamos funciones estilos y otros modulos y las ejecutamos para que sean compilados

// Estilos

import './scss/style.scss';

// Funciones

import {toggleNav, scrollNav, animaHeader, colorHeader, showButtonUp, upToTop} from './js/nav/nav'

toggleNav()
